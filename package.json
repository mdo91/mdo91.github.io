{
  "name": "mdo91.github.io",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "generate": "tsx scripts/generate-pages.ts",
    "dev": "npm run generate && vite",
    "build": "tsc && npm run generate && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vite": "^6.0.7"
  }
}
