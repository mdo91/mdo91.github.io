/**
 * Typed configuration for the embedded survey form.
 * Kept in sync with index.html iframe attributes.
 */
export const surveyFormConfig = {
  src: 'https://app.youform.com/forms/edg1gux5',
  loading: 'lazy' as const,
  width: '100%',
  height: '100%',
} as const;
